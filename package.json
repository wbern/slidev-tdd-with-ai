{
  "name": "slidev-tdd-with-ai",
  "private": true,
  "scripts": {
    "dev": "slidev slides.md --open",
    "build": "slidev build slides.md --out dist --base /slidev-tdd-with-ai/",
    "build:local": "slidev build slides.md --out dist --base /",
    "serve": "pnpm build:local && npx serve dist -p 3000",
    "preview": "npx serve dist -p 3000"
  },
  "dependencies": {
    "@slidev/cli": "^52.0.0",
    "@slidev/theme-default": "latest",
    "@slidev/theme-seriph": "^0.25.0"
  }
}
